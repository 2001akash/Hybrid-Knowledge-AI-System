# 🌐 Blue Enigma — Hybrid Knowledge AI System

This project combines:
- **Neo4j** → graph-based structured knowledge  
- **Pinecone** → semantic vector retrieval  
- **OpenAI GPT** → reasoning & natural language generation  

## 🏗️ Setup Instructions

### 1️⃣ Clone & Install
```bash
git clone <repo_url>
cd blue_enigma_hybrid_ai
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
  
